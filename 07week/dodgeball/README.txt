map through the array and target person by id and add them to the player list have the person removed from the people list.
so that players arent duplicated target ul, = null, and redisplay the list

mapped the array, used innerHTML ids to detrermine what list to put player in (blue or red). spliced specific player by id and pushed to that team as an array. 

created remove button, when clicked used slice on team array and pushed back to player list array